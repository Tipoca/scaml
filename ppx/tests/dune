(executables
  (names test)
  (flags -w -32)
  (preprocess (pps ppx_curried_constr)))

(alias
 (name test)
 (deps test.exe)
 (action (run ./test.exe)))
  
(alias
 (name runtest)
 (deps (alias test)
       ))
  
